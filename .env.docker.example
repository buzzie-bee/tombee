# SMTP configuration for contact form (Docker Compose)
# Uses the tombee-mailhog service name for Docker internal DNS
SMTP_HOST=tombee-mailhog
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=hello@tombee.io
CONTACT_EMAIL=hello@tombee.io
